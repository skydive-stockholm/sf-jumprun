# DEPRECATED: These environment variables are no longer required.
# The app now stores settings (Mapbox API key, map center) in data.json
# via the Settings panel in the admin UI.
#
# The following are only needed for Azure Table Storage upload:
AZURE_STORAGE_ACC=""
AZURE_STORAGE_TABLE=""
AZURE_SAS_TOKEN=""
